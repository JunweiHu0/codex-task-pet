{
  "_note": "PROTOTYPE — UNVERIFIED. This hooks manifest is a BEST GUESS. No installed Codex plugin on this machine declares hooks, and no hooks.json schema is documented here, so the field names / event names / invocation format below WILL likely need to change once the real Codex hooks schema is confirmed. Do NOT treat this as installable. See ../README.md.",
  "version": "0.1.0-prototype",
  "hooks": {
    "PreToolUse": { "type": "command", "command": ["node", "${PLUGIN_DIR}/hooks/pre-tool-use.js"] },
    "PostToolUse": { "type": "command", "command": ["node", "${PLUGIN_DIR}/hooks/post-tool-use.js"] },
    "PermissionRequest": { "type": "command", "command": ["node", "${PLUGIN_DIR}/hooks/permission-request.js"] }
  }
}
