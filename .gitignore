# dependencies
node_modules/

# Local cloned reference project — NOT part of this repo.
# (kept locally for reference; the app does not depend on it)
/SuperNoNo/

# local tooling / editor / agent state
.claude/
.obsidian/

# local Codex adapter runtime state
adapters/codex-desktop/notify-wrapper.config.json
adapters/codex-desktop/notify-observed.json

# build output
dist/
build/

# logs & OS cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db
